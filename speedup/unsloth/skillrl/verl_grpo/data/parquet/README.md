# verl_grpo parquet data

These files are generated by `../prepare_zoomearth_parquet.py` for the bbox-only
ZoomEarth GRPO setup.

## Files

- `train.parquet`
  - source: `/root/autodl-tmp/VQA/speedup/unsloth/skillrl/stageA/data/splits/rl_train.jsonl`
  - raw rows: 1966
  - kept rows: 1472
  - split role: train

- `dev.parquet`
  - source: `/root/autodl-tmp/VQA/speedup/unsloth/skillrl/stageA/data/splits/rl_dev.jsonl`
  - raw rows: 534
  - kept rows: 398
  - split role: eval/dev

Only rows with `type in {object, region}` and a four-value bbox are kept. Global
QA rows are excluded because this run's agent policy is bbox-only and requires a
zoom step.

Cached global images are stored under `../cache/global512`.
